# Review log

One row per pre-merge review: date, branch, class, reviewer tier, verdict,
catches (blocker/should-fix/nit), escapes, notes.

| Date | Branch | Class | Tier | Verdict | B/SF/N | Escapes | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-08-23 | tack-458-audit-host-commands (fe7585e+dab9845) | security/operator-cli | strongest-model adversarial | MERGE-READY | 1/0/2 | none | Blocker (uncapped, unresumable audit query) fixed as dab9845 and re-verified; RedactPIIRefs mutation caught by the DSN-gated test. |
| 2026-08-23 | tack-457-membership-gate (42f2673+fbbf852, re-verified as 426dca7+2d5c89c) | security/authz | strongest-model adversarial | MERGE-READY | 0/2/2 | TACK-459 (chain-append concurrency test crashes single-node throwaway YB; pre-exists on main) | Both should-fixes (index-trusting search, foreign relationship endpoint render) fixed as 2d5c89c; both membership-gate mutations caught red/green. |
| 2026-08-28 | tack-461-zero-org-backfill (9e7c570+41c8ad3+7effe9f, re-verified as +910729c) | security/ledger-rewrite | strongest-model adversarial | MERGE-READY | 1/1/3 | row.go Row doc comment named the removed MCP tool surface (TACK-456); fixed in 910729c | Blocker reproduced on the throwaway DB (deriveBackfillTarget refused the system org production's operator rows carry, so the command refused its target deployment) and fixed as 910729c with the derivation test; exemption red/green re-verified against the new test; six mutations red/green total; moved v1-shape nil-org row proven to verify as v3. |
| 2026-08-30 | tack-463-stream-verify (703c24d) | correctness/scale | session adversarial (author-run playbook) | MERGE-READY | 0/1/0 | none | Fixes the OOM that killed audit verify on the 420,562-row production bundle. Should-fix caught and corrected during review: the first memory test was NOT load-bearing, since a 64MB headroom constant let the mutation (retaining every decoded Row) pass; tightened to 4x the link record plus 4MB, after which the mutation fails at 19.5MB against an 11.9MB budget and the real implementation passes at 5.7MB for 20k rows, projecting 120MB at production scale against the 512MB ops-sidecar limit. Gap and break semantics unchanged, including gaps counted not failed; all eight pre-existing verify tests still pass, covering tamper, hash-version downgrade, version relabel, and planted-key rejection. Merge-tree clean against live main a12818e. UNPROVEN until after merge: the real 420k bundle at /root/b462prod, which the operator directed be verified from a main build on prod rather than a branch build. |
| 2026-08-29 | tack-462-absorb-phantom (3b00408+9f98207) | security/ledger-rewrite | session adversarial (author-run playbook) | MERGE-READY | 0/0/0 | none | Both exemption paths mutation-proven red/green on the throwaway DB (absorb skip removed broke TestDeriveSoleOrgWithExemptions; acknowledge skip removed broke TestMoveAbsorbedAndAcknowledgedRows); merge-tree clean against live origin/main d1ae0c9; all four new tests ran (4 RUN / 4 PASS) plus the three pre-existing backfill suites green; absorbed-org guard deliberately skips chunk-actor checks because the operator names the org wholesale, documented on AbsorbedOrgGuard. Second reviewer is the PR bot. |
| 2026-08-30 | tack-455-deleted-subject-keys (b69cdc9+54d7cb0) | correctness/ledger-reconstruction | session adversarial (author-run playbook) | MERGE-READY | 0/0/1 | none | Unblocks the production reference-key backfill, which refuses at 1619 derived against 1620 recorded. Premise verified against production before writing code, not assumed: the post-window ledger holds exactly two node.delete rows, one tack_delete_comment and one tack_delete_issue, both outcome NULL with entity kind mcp_tool and the zero id, which is the shape the derivation now reads. Attribution proven by exhaustion rather than assumed: 120 issues were created between the repair window and the deletion, so the deleted issue is not provably pre-window on its own, but the ledger holds exactly one reference-bearing deletion in the whole window and exactly one key is missing today, so they are the same node. Three mutations red/green (failed-delete skip, type filter, truncated-read refusal) plus one on the datagen shape (mcp_tool entity kind). Nit found and fixed during review: the first type-filter test was NOT load-bearing, because a reference-free type renders no template and so adds no event either way; the assertion moved to the subject set, where the filter actually decides, and the mutation then fails. Verified the app container carries AUDIT_READER_DSN on production, since the derivation now refuses without it. UNPROVEN until the QA window opens: the testbed pass and the production dry run. |
| 2026-08-30 | tack-466-deleted-evidence-node (c4c36f6) | correctness/ledger-reconstruction | session adversarial (author-run playbook) | MERGE-READY | 0/0/0 | none | Fixes a defect the TACK-455 QA proof surfaced live: deleting one of the 104 renamed evidence nodes aborted the whole reconstruction with node_resolve_failed before any class was counted, so the operator got a resolve error rather than a count. The rename still happened, so the event is still owed; only the node's type is lost, and the event now carries subject_absent rather than guessing. Derived stays 104 and AbsentSubjects reports the gap, so a deletion never shrinks the reconstruction. Two mutations red/green, the first reproducing the exact production-shaped failure message ("resolve evidence node 019dc5ed-eac1-7ab4-b86b-cebc6ce06de8: not found"), the second proving a real read failure still refuses rather than being swallowed as absence; two more tests pin that all-nodes-absent refuses, because nothing would then name the org. Pre-existing suites green; the internal/audit failures on the shared throwaway are TACK-459's known crash, reproduced before this change and clean after a database restart. UNPROVEN until a QA window: the testbed run, which needs a deleted renamed node on a repair corpus. |
| 2026-08-30 | tack-439-refuse-unrecorded (65659e9) | security/audit-availability | session adversarial (author-run playbook) | MERGE-READY | 0/0/0 | none | Closes the epic's own goal sentence on the server side: a recorder that failed to open was replaced by one that discarded every event, so an unreachable ledger produced a healthy-looking server that recorded nothing, and a caller could not tell. Startup now refuses on every failure, with one exception the operator declares through AUDIT_ALLOW_UNRECORDED. Two design points that needed evidence rather than reasoning: the Kafka client connects lazily, so construction proves nothing and the refusal needs a real reachability check; and one attempt is wrong on a cold start, where compose brings the broker up alongside the app, so the check retries inside a configurable budget and only then refuses, which is the crash-loop TACK-453 removed from the provision path. The uninstalled default at both request boundaries also changed from discard-and-succeed to refuse-and-log. Four mutations red/green: removing the reachability check, removing the acknowledgement gate, restoring the noop substitution on the Yugabyte path, and collapsing the retry budget to a single attempt (which fails the slow-broker test). Verified before writing code that prod and QA both set AUDIT_KAFKA_BROKERS, so the refusal cannot brick either. One compose change was tried and reverted: kafka is a profiled service, so the app cannot depend_on it in the base file; the retry budget covers ordering instead. UNPROVEN until deployed: the testbed demonstration of a broken backend refusing, which the ticket requires be observed rather than read. |
| 2026-08-30 | tack-verify-manifest-count (5491887) | security/verifier-input-trust | session adversarial (author-run playbook) | MERGE-READY | 0/0/0 | none | Found by the backup session's scale audit, confirmed against source, ticketed as TACK-469. VerifyBundle sized its link slice from the manifest's row_count before anything about the manifest was trusted: the signature check ran first but only set a flag, so a bundle claiming a hundred million rows over a two-row file made the verifier allocate for the claim on the line before it would have rejected the bundle, and offline inspection of a foreign bundle is exactly what the function is for. The same file never compared the declared count to the rows the file held, so a validly signed manifest that disagreed with its own file, which is what a truncating exporter produces (TACK-470), passed. Fix: no allocation from the manifest, the declared count carried into the report, and Err failing on a count that differs from RowsScanned independent of the signature. export_verify.go was over the 200-line limit, so the manifest read and the chain walk moved to their own files unchanged. Two tests, both mutation-checked: restoring the preallocation panics the first with makeslice cap out of range; disabling the count check fails the second on a correctly signed bundle whose only defect is the count. The drill test's report literal gained the new field. Review then found five defects in the moved code, all real and all fixed with mutation-checked tests: a row from another org was not compared to the manifest org (an intact foreign row verifies row by row, since its hash is valid for its own org); a repeated sequence counted as a gap rather than a break, so a replayed row passed; the manifest was read whole with no size bound; unknown manifest keys were dropped rather than refused, the same rule rows already follow; and a wrong-length public key panicked inside ed25519.Verify instead of failing the signature check. Not proven on any testbed: the ticket's acceptance is a unit-level property of an offline verifier, and the testbed proof for this class is the export run under TACK-470. |
| 2026-08-31 | tack-452-partial-repair-record | correctness/audit-completeness | session adversarial (author-run playbook) | MERGE-READY | 0/0/0 | none | TACK-452: a reference repair that failed partway left every rename, counter seed, and key claim it had already applied with no ledger row naming it, and a rerun could not recover the record because a renamed node is no longer a duplicate and never appears in a later plan. Four places dropped the work: renumberDuplicateGroup returned nil on the first failure in a group, seedReferenceCounters and writeAllReferenceKeys did the same, and the command discarded the report entirely when the run returned an error. All four now carry the applied work up, and the command records it through the existing idempotent outbox before returning the run's error; deterministic event ids mean a later successful rerun re-records rather than doubles. Three design points, the last two found by review rather than by me: the key stage narrows its claimed set to the nodes whose write actually landed, because reporting every claimed key would invent changes that never happened; the audit write runs on a context detached from cancellation with its own 30 second deadline, because a run killed by an interrupt would otherwise hand the same dead context to the mandatory record and fail exactly when the applied work most needed one; and when both the run and the recording fail both errors are returned joined, since returning only the run failure hides that applied changes are still unrecorded. The fallback log reversed twice before landing right: it now writes every applied identity in batches of fifty rather than a sample, because a sample drops exactly the renames no rerun can rediscover, while one line per item is a storm that outlives the command's own deadline. Also added, because the acceptance asks for a fault: --fail-after-renames stops a run after N applied renames so a partway failure can be produced against a real store, refused unless TACK_DATAGEN_ALLOW_TARGET is qa or local. Nine tests, each mutation-checked one at a time. One mutation initially survived, which exposed that renumberDuplicateGroup had no test at all because it needs concrete FDB stores; the loop was extracted behind a rename function so the partial return is now provable without a database. Not proven on any testbed: the ticket's second acceptance is a run failed mid-flight against a real store, which is a QA fault-injection run. |
| 2026-08-31 | tack-340-verb-coverage | correctness/audit-completeness | session adversarial (author-run playbook) | MERGE-READY | 0/0/0 | none | TACK-340, claim 7 of the audit epic: every declared verb has an emitter or is removed. An audit of all 80 declared verbs found eleven that nothing recorded, and one of them, mcp.resource_read, was recorded on its own ticket as shipped in PR 81 when `git log -S` shows the constant never appeared outside verbs.go in any commit. The existing coverage_test.go only checks tool-to-verb (a listed tool resolves to a verb) and cannot see a verb with no emitter, which is why the drift went unnoticed for months. Added the missing direction: TestEveryDeclaredVerbHasAnEmitter parses verbs.go for declared constants, walks the repository for non-test references, counts an entry in staticToolVerb or perTypePrefixVerb as an emitter because ToolVerb routes those to the recording wrapper, and fails otherwise unless the verb sits in an unemittedVerbs map with a reason naming its ticket. The first run of that test failed by counting stateChangeVerbs as routing, which marked five unemitted verbs as emitted; stateChangeVerbs classifies rather than routes, and the fix is the reason the two kinds of map are now distinguished by name. Wired mcp.resource_read: the getting-started handler took a context and ignored it, so reading the resource was a product action that left no row. Removed mcp.session_start and mcp.session_end, which cannot be emitted because the transport is stateless and no session boundary exists. Fixed two stale claims found while working: verbs.go said coverage_test.go fails the build when a registered tool lacks a mapping, which no test enforces, and the emission test initially called the recording helper directly. That last one is the finding worth keeping: mutating the handler to drop its record left the test green, because a test that calls the helper proves the helper works and not that anything calls it, which is the exact shape of the defect being fixed. The test now drives the registered handler and fails on that mutation. A third mutation, orphaning the emitter entirely, passes the verb test and is caught by lint-deadcode instead; the test comment says so rather than implying coverage it does not have. Not proven on any testbed: a ledger row for a resource read needs an MCP call against QA. |
| 2026-08-31 | tack-340-datagen-resource | correctness/audit-completeness | session adversarial (author-run playbook) | MERGE-READY | 0/1/0 | none | Follows #179 and fixes a rule that change broke. AGENTS.md requires any new audit verb path to add `ops qa datagen` coverage in the same change; #179 wired the mcp.resource_read emitter and added none, so the verb still had no generated traffic and no testbed run could produce a row for it. That is the same gap in a second form: the emitter existed and nothing exercised it. The generator now reads tack://getting-started once per seed run, as the first workspace's first actor, through the same authenticated boundary its tool calls use, and skips with a logged reason when the scale generates no actor. Found while trying to prove #179 on QA and discovering there was no way to: QA runs ENV=production after the TACK-261 cutover, the deployment renders no SEED_API_TOKEN, and TACK-472 records that no command mints a token for an existing user, so no hand-made MCP call was possible. The generator holds its own minted tokens, which makes it the only path to the row. Not proven on any testbed until this deploys and a seed run's mcp.resource_read row is read back from audit.events. |
| 2026-09-02 | tack-336-producer-spill | correctness/audit-availability | session adversarial (author-run playbook) | MERGE-READY | 0/1/0 | none | Claim 5 of the audit epic, producer half of TACK-336 plus TACK-320. Premise verified on production before writing code: the 2026-07-06 gap left eight issue creations in FoundationDB with no ledger row because the producer dropped every event its broker refused (TACK-335). The FoundationDB operator outbox already had a reader, a relay in the audit-consumer draining it to Kafka, and an integration test, and nothing in production wrote to it; the only writer was a test helper. Added the writer as OpsOutboxStore.Append, and a SpillRecorder that wraps the Kafka producer: a refused event lands in the outbox with its id and time intact, the relay delivers it when the broker returns, and the caller is told nothing went wrong because nothing did. Logging is per outage rather than per event: the producer warns once when the broker starts refusing and logs Info once when it recovers, the spill logs Error once with the first refusal and Info once with the count owed, and a spill that itself fails is the only per-event Error, since that is the only real loss. Wired in the runtime around the Kafka recorder only, because the synchronous Yugabyte path writes to the same database the relay would deliver into and has no second store. Should-fix caught during review: the first fake broker echoed only the topic name, and Produce v13 keys the reply by topic id, so kgo re-enqueued the batch forever and the tests hung; the fix is in the test with the reason stated. Two house lints then collided at the producer, wrapcheck wanting the wrap and log-and-wrap wanting a Warn beside it, which is the per-event flood in a different colour; resolved by the once-per-outage Warn rather than by weakening either rule. Five mutations red/green: spill never appends, spill logs Error per event, producer logs Error per event, producer warns on every refusal, plus the pre-existing monitoring test updated to the first-refusal line. Integration test now writes through Append rather than the deleted helper. UNPROVEN until deployed: the QA demonstration, which is stopping Kafka on the testbed, making product writes, restarting Kafka, and reading the spilled events back from audit.events with the relay's count matching. |
| 2026-09-03 | tack-475-shape-restore | correctness/testbed-corpus | session adversarial (author-run playbook) | MERGE-READY | 0/0/0 | none | TACK-475: the repair-shape generator reported collisions 102 on QA while creating none, because a re-run skipped every node that already existed and the earlier repair had left them all unique; the repair it exists to exercise then renamed nothing and exited 0. The generator's own comment claimed a re-run rewrites the same corpus. It now does: an existing node whose sequence differs from the shape is put back through UpdateAtomic with its reference claims released, so the duplicate scan, which renders references from props, sees the collision again and the next repair can allocate afresh. The report gains nodes_restored and live_collisions, the latter counted after writing through the repair's own duplicate scan, and a commit refuses when live_collisions differs from the shape's count. Unit test on the refusal; the restore path has no unit-level proof, since the ops tests carry no in-memory node store and the FoundationDB test image cannot run on this host, so the QA run is the proof: nodes_restored above zero, live_collisions 102, and a repair that then renames 103. UNPROVEN until that run. |
| 2026-09-03 | tack-472-token-mint | security/auth | teammate adversarial (review-472, Fable) plus pr-review-agent two rounds | NOT-READY at 102837f, fixes applied | 0/5/5 | none | TACK-472 adds ops auth token-create, token-list, token-revoke. Reviewed at 102837f. Should-fix, all verified against source and fixed in the next commit: revoke retry blocked by a deterministic intent event id under WriteOutbox (now every attempt carries its own id in every row key, so a retry records its own intent and outcome); raw token echoed into a log line and the choke-point error row when pasted as --id (the value is no longer echoed anywhere); one-sided email lowercasing against a case-sensitive users.email (exact match after trim, like the seed); a deactivated user's token unrevocable while still valid (holder read without the active filter); org rule contradicted the bearer middleware (nil org for zero or several memberships, matching auth.token_used rows). pr-review-agent round two, also fixed: a write that fails after its intent row now records an error outcome for both issue and revoke; the withdrawal of an unconfirmed or unreported token is a recorded revocation on a context detached from cancellation rather than a bare delete, so an outcome write that committed before its error came back is answered by revoke rows. Host unit suite and make build green; integration suite not runnable on arm64 (amd64-only fdb image segfaults under qemu), judged by CI. UNPROVEN until the QA run. |
| 2026-09-03 | tack-472-token-mint | security/auth | teammate adversarial (review-472, Fable), final pass | MERGE-READY | 0/0/4 | none | At cfa91fd. Retry test traced red at 102837f (outbox primary key 23505 on the second intent) and green at the tip. Nits applied afterwards: an insert that reports failure is reconciled by a recorded revocation of the preselected id, and an inconclusive lookup is reported as unverified rather than as a clean failure; the test helpers moved to their own file for the line limit. Not run by the reviewer: unit and integration suites (arm64 host); build and unit suite green on the author's host, integration judged by CI. |
| 2026-09-03 | tack-459-single-node-throwaway | test-infra | pr-review-agent and Copilot, three rounds | MERGE-READY at 556cfad, merged 027b72a | 0/5/1 | none | TACK-459: the chain-append concurrency test crashed the single-node throwaway YugabyteDB. Root cause reproduced and isolated to the engine's arm64 build (pggate garbage-length check, then a fourteen-year clock-skew abort); the amd64 build passes every run. Change: the test compose yugabyte service pinned to linux/amd64, scripts/test-audit-db.sh plus make test-audit-db running the gated packages serially inside the runner against that database alone, and the host-command test's throwaway role given a password so it holds under password authentication. Findings applied: URL-encode the password (bytewise), a query-loop readiness gate instead of the compose wait flag, an arithmetic loop, the selected runtime in the timeout hint. Acceptance counted four times from wiped and warm compose states; removal check on the retry loop red then green. |
| 2026-09-03 | tack-336-consumer-dlq | audit/durability | author-run adversarial pass, engine-backed test, one mutation | BUILT | 0/0/0 | none | TACK-336 consumer half plus TACK-240 reshaped. A ledger-refused insert no longer fails the batch: each record projects in a savepoint, a refusal dead-letters the record with the ledger's reason in the same transaction that advances the offset, and a chain conflict still fails the batch. Replay is ops audit dlq replay, which re-publishes rows to the topic under a header naming the row; the consumer, still the single writer, deletes the row when the event lands or counts the attempt when it fails again (migration 011: attempt_count, last_attempt_at, UPDATE and DELETE for audit_writer). ops audit dlq inspect groups rows by failure and logs an error line when non-empty. The consumer sets retention.ms on the topic at startup (default one year, AUDIT_CONSUMER_TOPIC_RETENTION). Engine-backed test TestConsumerDeadLettersARefusedInsertAndReplaysIt passes against YugabyteDB through the compose runner; mutation restoring the old behaviour (refused insert fails the batch) makes it red at "waited for 1 rows; got 0". CI gains the make test-audit-db step so these tests run at all. UNPROVEN until the QA run. |
| 2026-09-03 | tack-336-consumer-dlq | audit/durability | teammate adversarial (review-336, Fable) plus pr-review-agent two rounds | NOT-READY at 33ec8ef, fixes applied | 1/7/5 | consumer never committed Kafka group offsets since a5aec6d (MarkCommitRecords without AutoCommitMarks); countRowsForGroup correlated-subquery bug in consumer_test.go | Reviewer reproduced on YugabyteDB through test-audit-db: green run, mutation A (refused insert fails the batch) red at "waited for 1 rows; got 0", mutation B (resolve never deletes) red at "dead letters after the replay = 2", a hostile replay header wedged the partition. Blocker fixed: the loop commits each batch with CommitRecords right after the ledger transaction (the client refuses DisableAutoCommit together with AutoCommitMarks), so a restart resumes where the ledger transaction left off; the retention raise to one year made the old re-scan operational. Should-fix applied: an unparseable replay header is logged and the record projected fresh; the replay sets the producer's partition key from the payload; a retention alter failure logs and continues; replay order is fewest attempts first; countRowsForGroup counts by org; a dead-lettered counter; migration-before-image stated in the first-boot list. Bot round two: replay origins are verified by stored payload before any delete or attempt count. Test now asserts exactly one attempt and adds the hostile-header case; the far-future partition is computed, not fixed. Declined with reasons on the thread: a replay lease (a duplicate replay is an errAlreadyProjected no-op; a still-failing one counts an attempt on its own row); a dlq discard command (owed). UNPROVEN until the QA run. |
| 2026-09-04 | tack-336-consumer-dlq | audit/durability | teammate adversarial (review-336b, Fable) plus pr-review-agent rounds three and four | NOT-READY at 3b8a88a, fixes applied | 1/2/4 | local test database drifted by the 010 renumbering (main's 010 recorded, never ran; recreated with make test-fdb-down); consumer_fetches.go shift conversion failed CI's gosec G115 while local lint passed | Blocker reproduced by the reviewer: a null-value Kafka record (tombstone) failed the dead-letter insert (pgx binds nil as NULL, payload NOT NULL, 23502), the new rewind redelivered it, and the partition stalled for ever; fixed by storing an empty payload (recordPayload), pinned by TestConsumerSurvivesHostileRecords. Rewind had no test (mutation removing SetOffsets stayed green): TestConsumerRewindsAFailedBatch takes the writer login's privileges away, observes nothing committed, restores them, and asserts both records land and the offset commits. Bot round four applied: UPDATE grant scoped to attempt_count, last_attempt_at, error; attempt_count moves only on a verified replay (a redelivered original is DO NOTHING); back-off doubles in a loop with no shift conversion; resolve logs a warning when no row was deleted; metric comment covers undecodable payloads. Help text now says fewest attempts first. Ten consumer tests green on a fresh YugabyteDB through the runner. UNPROVEN until the QA run. |
| 2026-09-04 | tack-336-consumer-dlq | audit/durability | teammate adversarial (review-336b, Fable), final pass | MERGE-READY at fb9ccc8 | 0/0/3 | none | Rewind mutation traced red at the new tip (rewind test, "never re-fetched after the fault cleared") and green unmutated three times in a row with five 42501 failures before recovery; column-scoped UPDATE verified in information_schema and exercised by the replay test; effective isolation repeatable read so the EXISTS-then-UPDATE race retries rather than dropping. Nits applied in the next commit: the counting UPDATE checks its row count and falls back to the record's own key with a warning; the rewind test closes its consumer on every path. Noted, no action: a database that applied the earlier text of 011 would keep the table-wide UPDATE grant; none exists. 10/10 consumer tests, lint, fresh build green. UNPROVEN until the QA run. |
| 2026-09-04 | tack-437-signer-set | audit/integrity | author-run adversarial pass, engine-backed test | BUILT | 0/0/0 | two key-identifier derivations existed (notarizer hashed the public key, the export named its raw bytes) | TACK-437: one identifier (audit.KeyIdentifier), signing_host on every notarization (migration 012, AUDIT_CONSUMER_SIGNING_HOST), a per-environment valid signer set (AUDIT_VALID_SIGNERS), `audit signers` that reads every notarization through the reader role and rejects any signed outside the set naming identifier and host, and `audit verify` rejecting a manifest signer outside a configured set. Engine test on YugabyteDB: a rogue key's rows rejected and named with their host, a forged identifier over a foreign signature fails the signature check, admitting the identifier accepts its rows as unverified here. Command refuses to run without a set. UNPROVEN until the QA run. |
| 2026-09-04 | tack-437-signer-set | audit/integrity | teammate adversarial (review-437, Fable) | NOT-READY at ef327b2, fixes applied at 6736f6a | 0/5/6 | the configs deploy rendered neither new variable (configs PR 315 renders them); without a local key `audit signers --signers` exited 0 having verified no signature | Reviewer reproduced on YugabyteDB: both mutations red (Allows-true fails three tests, signature check removed fails the engine test), five unmutated green, the reader role selects signing_host and is refused INSERT. Applied: the command refuses without a local key (ErrNoLocalKey) and without a set; rows under allowed identifiers this host holds no key for fail the report unless --allow-unverified is passed; local_signer is always printed; every host a rejected signer claimed is reported as a set; the scan no longer sorts; the unconfigured-set line is error level; audit verify moved to its own file; the migration and config comments describe the host as a claim beside the signature, not proof. Not done: folding host and identifier into the signed bytes (a signature version change). UNPROVEN until the configs merge and the QA run. |
| 2026-09-04 | tack-437-signer-set | audit/integrity | teammate adversarial (review-437, Fable), second pass | NOT-READY at 6736f6a, fixes applied at 84bbe47 | 0/2/3 | none | Reviewer verified every round-one fix and traced the --allow-unverified gate red by mutation. Applied: the report types and refusal errors moved to signers_report.go; a local key outside the set is refused (ErrLocalKeyNotInSet) so an acknowledgement can never clear rows having verified nothing; the rejected span is asserted from row times with rows inserted out of order across two orgs; the command runs against the migrated ledger in a test that pins --allow-unverified and --since through the reader. UNPROVEN until configs PR 315 deploys and the QA run. |
