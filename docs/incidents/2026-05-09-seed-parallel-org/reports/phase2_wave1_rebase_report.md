# Phase 2 Wave 1 Rebase Report

## 1. Worktree path and branch

- Working directory: `/Users/agoodkind/Sites/tack`
- Branch: `phase2-wave1-rebase`
- Base: `d2da217 Pass FDB_CLUSTER_FILE env var to fdbbackup sidecar` which
  contains `b0fc2a0` (backup rebuild) and `23ad44a` (Phase 1 audit WAL).
- Source of Phase 2 work: `/Users/agoodkind/Sites/tack-phase2-wave1-wip`
  on branch `phase2-wave1-wip` (HEAD `dd430c9`, all Phase 2 changes were
  uncommitted in that worktree).

Note on isolation: the harness placed me at `/Users/agoodkind/Sites/tack`
and `EnterWorktree` reported the agent is already isolated, so writes go
through whatever copy-on-write the harness provides. The branch
`phase2-wave1-rebase` was created from current main HEAD before any
edits so the result can be diffed against `main` cleanly.

## 2. Files copied from phase2-wave1-wip (new)

Verbatim copies, no edits:

- `cmd/audit-consumer/main.go`
- `internal/audit/consumer.go`
- `internal/audit/consumer_test.go`
- `internal/audit/dual.go`
- `internal/audit/dual_test.go`
- `internal/audit/kafka_recorder.go`
- `internal/audit/kafka_recorder_test.go`
- `migrations/003_audit_kafka_consumer.sql`
- `migrations/004_audit_events_event_id_uniq.sql`
- `migrations/005_audit_events_v2_sibling.sql`

## 3. Files merged from phase2-wave1-wip

- `cmd/server/main.go`: kept Phase 1 WAL wiring intact and split
  `buildAuditRecorder` into `buildAuditRecorder` plus the new
  `buildAuditWALRecorder` and `wrapAuditWithKafka` helpers exactly as
  phase2-wave1-wip had them. Phase 2 doc-comment block added above
  `buildAuditRecorder`. No other touches.
- `internal/config/config.go`: NO CHANGE NEEDED. Current main already
  contains the full set of `AUDIT_KAFKA_*` and `AUDIT_CONSUMER_*` fields
  (they landed as part of commit `23ad44a`). Verified by diffing against
  the wip version: identical.
- `docker-compose.yml`: added the four `AUDIT_KAFKA_*` env vars to the
  `app` service, then appended the `kafka`, `seaweedfs`, `clickhouse`,
  and `audit-consumer` service blocks after `temporal-ui`, and added
  `kafka-data`, `seaweedfs-data`, `clickhouse-data` to the `volumes`
  block. Existing services and the IPv6 network section are unchanged.
- `.env.example`: appended the `KAFKA_CLUSTER_ID`, `AUDIT_KAFKA_*`, and
  `AUDIT_CONSUMER_*` blocks before the existing backup-overrides
  section. Main's backup-override block (which is more complete than the
  wip version's) is preserved verbatim.
- `Makefile`: added the `.PHONY: audit-consumer` target between the
  `seed-audit-roles` target and the `backup-pull` target. Main's
  `backup-content-check` and `backup-restore-test` targets are
  untouched.
- `go.mod`: added `github.com/ClickHouse/clickhouse-go/v2 v2.46.0`,
  `github.com/twmb/franz-go v1.21.1`, and
  `github.com/twmb/franz-go/pkg/kfake v0.0.0-20260504210720-963be6fd5989`
  to the direct-require block. The indirect block was regenerated by
  `go mod tidy`.

`internal/audit/wal.go`, `internal/audit/wal_test.go`, and
`internal/telemetry/metrics.go` were NOT touched, per the constraint.

## 4. go mod tidy outcome

`go mod tidy` ran clean and added the expected indirect dependencies
(`ClickHouse/ch-go`, `go-faster/city`, `go-faster/errors`,
`klauspost/compress`, `paulmach/orb`, `pierrec/lz4/v4`, `segmentio/asm`,
`shopspring/decimal`, `twmb/franz-go/pkg/kmsg`, `go.yaml.in/yaml/v3`,
`golang.org/x/crypto`). go.sum was regenerated.

## 5. make build outcome

`make build` (which runs `go vet`, `golangci-lint`, `gocyclo`,
`deadcode`, `staticcheck-extra`, `govulncheck`, then `go build -tags
fdb`) passed with zero new findings:

```
golangci-lint: OK   (New findings: 0; Saved findings now fixed: 147)
gocyclo: OK         (Functions over complexity limit 30: 0)
deadcode: OK        (New findings: 0)
staticcheck-extra: OK (New findings: 0; Saved findings now fixed: 26)
No vulnerabilities found.
go build -tags 'fdb' -o dist/tack ./cmd/server
built: dist/tack
```

The "Saved findings now fixed" lines indicate baseline files have stale
entries that could be refreshed with `make lint-golangci-baseline` and
`make staticcheck-extra-baseline`. I did not run those refreshes since
they were unrelated to the Phase 2 changes.

## 6. make test outcome

`make test` (which is `go test ./...` with parallelism set) passed:

```
ok  goodkind.io/tack/internal/adapters/foundationdb
ok  goodkind.io/tack/internal/adapters/mcp/tools
ok  goodkind.io/tack/internal/audit                63.131s
ok  goodkind.io/tack/internal/domain/node
ok  goodkind.io/tack/internal/ops
ok  goodkind.io/tack/internal/service
ok  goodkind.io/tack/internal/test/integration
```

Verbose run of just the Phase 2 audit tests confirmed:

- `TestKafkaRecorderProducesAndCommits` PASS (uses `kfake` in-process
  broker; no external Kafka required).
- `TestKafkaRecorderReturnsErrorOnBrokerDown` PASS.
- `TestKafkaRecorderPayloadShape` PASS.
- `TestKafkaRecorderRequiresBrokers` PASS.
- `TestDualWritesToBoth`, `TestDualReturnsFirstError`,
  `TestDualReturnsBothErrors`, `TestDualPropagatesContextCancellation`,
  `TestDualNilRecorderRejected` all PASS.
- All five `TestConsumer*` tests SKIP cleanly with the message
  `AUDIT_CONSUMER_TEST_DSN unset; consumer integration tests skipped`,
  matching the env-var-gated contract requested.

No tests were silenced or stubbed.

## 7. Em-dash and double-hyphen scan

- Em-dash scan (`grep -P "[\x{2013}\x{2014}\x{2015}]"`) on every file I
  touched (the seven new Go and SQL files plus the six merged files):
  zero hits.
- Prose double-hyphen scan on the Go and config files: zero hits. The
  remaining `--` sequences are SQL comments inside the migration files
  and CLI flags inside the docker-compose `healthcheck` block (e.g.
  `--bootstrap-server`), which the rule explicitly allows.

## 8. Design decisions made en route

- The instruction said "current main has the Phase 1 `AUDIT_WAL_*` env
  vars; Phase 2 adds `AUDIT_KAFKA_*` and `AUDIT_CONSUMER_*`. Add only
  the Phase 2 fields." On inspection, current main HEAD already
  contains every Phase 2 field in `internal/config/config.go` because
  commit `23ad44a` landed them together with the WAL idle-rotation
  config. I left config.go untouched and noted it explicitly in
  section 3 rather than re-add identical fields.
- The pre-existing untracked file `docs/phase2-wave1-runbook.md` was
  not in the wip worktree and is not in the task's file list. I left it
  in place untouched.
- The wip branch's `.env.example` had an older, less complete
  backup-override block. I kept main's newer block and only inserted
  the Phase 2 (`KAFKA_CLUSTER_ID`, `AUDIT_KAFKA_*`, `AUDIT_CONSUMER_*`)
  blocks above it. No regression in backup overrides.
- I did not run `make lint-golangci-baseline` or
  `make staticcheck-extra-baseline` to clear "saved findings now fixed"
  entries because those baseline edits are out of scope for a Phase 2
  rebase and the agent-gate `oss20-block-baseline-bash-tampering` rule
  would have blocked manual edits anyway.

## Result

The branch `phase2-wave1-rebase` sits on top of current main with
Phase 1 (audit WAL) plus the backup rebuild intact, adds the Phase 2
wave 1 producer (Kafka recorder, dual recorder), the audit-consumer
binary and its three migrations, and the docker-compose plus env-var
plumbing for kafka, seaweedfs, and clickhouse. `make build` and
`make test` both pass. The branch is ready for review and commit.
