services:
  app:
    image: tack-server:latest
    build: .
    restart: unless-stopped
    dns:
      - 127.0.0.11
    ports:
      - "${PORT:-8000}:8000"
    environment:
      DATABASE_URL: postgres://${YUGABYTE_USER:?YUGABYTE_USER is required, set it in .env}:${YUGABYTE_PASSWORD:?YUGABYTE_PASSWORD is required, set it in .env}@yugabyte:5433/${YUGABYTE_DB:?YUGABYTE_DB is required, set it in .env}?sslmode=disable
      FDB_CLUSTER_FILE: /etc/foundationdb/fdb.cluster
      MEILI_URL: http://meilisearch:7700
      MEILI_MASTER_KEY: ${MEILI_MASTER_KEY:-tack-dev-meili-key-change-in-prod}
      TEMPORAL_ADDRESS: temporal:7233
      PORT: 8000
      ENV: development
      # Dev policy: noisiest level on, every rotated log kept forever.
      # Logs are append-only history we can grep against post-incident.
      LOG_LEVEL: ${LOG_LEVEL:-debug}
      LOG_MAX_BACKUPS: ${LOG_MAX_BACKUPS:-0}
      LOG_MAX_AGE_DAYS: ${LOG_MAX_AGE_DAYS:-0}
      LOG_MAX_SIZE_MB: ${LOG_MAX_SIZE_MB:-100}
      # On-disk paths inside the container. The /var/log/tack volume below
      # binds the host path for cross-restart persistence.
      LOG_JSON_FILE: ${LOG_JSON_FILE:-/var/log/tack/app.jsonl}
      LOG_TEXT_FILE: ${LOG_TEXT_FILE:-/var/log/tack/app.log}
    volumes:
      - /etc/foundationdb:/etc/foundationdb:ro
      - tack-logs:/var/log/tack
    depends_on:
      yugabyte:
        condition: service_healthy
      fdb:
        condition: service_healthy
      meilisearch:
        condition: service_healthy
      temporal:
        condition: service_healthy

  yugabyte:
    # 2024.2.x LTS is the first line that ships IPv6 support in yugabyted
    # (issue #24665, fixed in 2024.1.5.0). is_port_available is now
    # AF-aware and validate_hostname_ip uses ipaddress.ip_address. Two
    # bugs remain: yugabyted's auto-detect still calls socket.gethostbyname
    # (AF_INET only), and the IP_ANY constant has no `::` equivalent. The
    # wrapper below dodges both by reading the bridge v6 from /etc/hosts
    # and passing it as a literal --advertise_address.
    image: yugabytedb/yugabyte:2024.2.8.0-b85
    restart: unless-stopped
    command:
      - /bin/bash
      - -lc
      - |
        set -euo pipefail
        ADDR=$$(getent ahostsv6 "$$(hostname)" | awk 'NR==1{print $$1}')
        if [ -z "$$ADDR" ]; then
          echo "yugabyte wrapper: no IPv6 address found for $$(hostname) in NSS" >&2
          exit 1
        fi
        echo "yugabyte wrapper: advertising $$ADDR" >&2
        exec /home/yugabyte/bin/yugabyted start \
          --daemon=false \
          --advertise_address="$$ADDR" \
          --listen="$$ADDR" \
          --tserver_flags="ysql_pg_conf_csv=max_connections=100"
    environment:
      YSQL_DB:       ${YUGABYTE_DB:?YUGABYTE_DB is required, set it in .env}
      YSQL_USER:     ${YUGABYTE_USER:?YUGABYTE_USER is required, set it in .env}
      YSQL_PASSWORD: ${YUGABYTE_PASSWORD:?YUGABYTE_PASSWORD is required, set it in .env}
    ports:
      - "5433:5433"   # YSQL (Postgres-compatible)
      - "7000:7000"   # YugabyteDB master UI
      - "9000:9000"   # YugabyteDB tserver UI
    volumes:
      - yugabyte-data:/home/yugabyte/var
    healthcheck:
      test:
        - CMD-SHELL
        - |
          ADDR=$$(getent ahostsv6 "$$(hostname)" | awk 'NR==1{print $$1}')
          PGPASSWORD=${YUGABYTE_PASSWORD:?} ysqlsh -h "$$ADDR" -p 5433 \
            -U ${YUGABYTE_USER:?} -d ${YUGABYTE_DB:?} -c 'SELECT 1' -t
      interval: 10s
      timeout: 10s
      retries: 10
      start_period: 60s

  fdb:
    # Version is set by tack's Makefile FDB_VERSION variable, also baked
    # into the Dockerfile build args and the Go bindings pin in go.mod.
    # Bump all three together via make update-fdb VERSION=...
    image: foundationdb/foundationdb:${FDB_VERSION:-7.4.6}
    restart: unless-stopped
    environment:
      FDB_NETWORKING_MODE: container
      FDB_PORT: "4500"
      FDB_PROCESS_CLASS: unset
    volumes:
      - fdb-data:/var/fdb
      - /etc/foundationdb:/etc/foundationdb
      # Patched fdb.bash with IPv6 bracket handling. See fdb-overlay/fdb.bash.
      - /root/tack/fdb-overlay/fdb.bash:/var/fdb/scripts/fdb.bash:ro
    healthcheck:
      test: ["CMD-SHELL", "fdbcli --exec 'status minimal' 2>&1 | grep -q 'available'"]
      interval: 10s
      timeout: 10s
      retries: 10
      start_period: 30s

  meilisearch:
    image: getmeili/meilisearch:v1.12
    restart: unless-stopped
    environment:
      MEILI_MASTER_KEY: ${MEILI_MASTER_KEY:-tack-dev-meili-key-change-in-prod}
      MEILI_ENV: production
      # Inter-container DNS on this network resolves AAAA only. Without an
      # explicit v6 listen address, Meilisearch binds 0.0.0.0:7700 (v4 only)
      # and the app's connection to meilisearch:7700 fails with
      # "connect: connection refused" on the GUA. Bind to all addresses on
      # both stacks so v6-only callers connect successfully.
      MEILI_HTTP_ADDR: "[::]:7700"
    ports:
      - "7700:7700"
    volumes:
      - meili-data:/meili_data
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://[::1]:7700/health | grep -q 'available'"]
      interval: 5s
      timeout: 5s
      retries: 10

  temporal-db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER:     temporal
      POSTGRES_PASSWORD: temporal
      POSTGRES_DB:       temporal
    volumes:
      - temporal-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U temporal"]
      interval: 5s
      timeout: 5s
      retries: 10

  temporal:
    image: temporalio/auto-setup:1.26.2
    restart: unless-stopped
    environment:
      DB:              postgres12
      DB_PORT:         5432
      POSTGRES_USER:   temporal
      POSTGRES_PWD:    temporal
      POSTGRES_SEEDS:  temporal-db
    ports:
      - "7233:7233"
    depends_on:
      temporal-db:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "tctl --address temporal:7233 cluster health 2>&1 | grep -q SERVING"]
      interval: 10s
      timeout: 10s
      retries: 15
      start_period: 30s

  temporal-ui:
    image: temporalio/ui:2.34.0
    restart: unless-stopped
    environment:
      TEMPORAL_ADDRESS: temporal:7233
    ports:
      - "8080:8080"
    depends_on:
      - temporal

volumes:
  yugabyte-data:
  fdb-data:
  meili-data:
  temporal-db-data:
  tack-logs:

# IPv6-only bridge. The /96 carved out of the host's on-link 3d06:bad:b01::/64
# is NDP-proxied back onto eth0 by ndppd on CT 117. Containers source out as
# real GUAs because gateway_mode_v6 is routed (no masquerade). Inter-container
# DNS returns AAAA only because enable_ipv4 is false. Do not flip back to
# dual-stack: that defeats the forced-v6 contract.
networks:
  default:
    enable_ipv6: true
    enable_ipv4: false
    driver_opts:
      com.docker.network.bridge.gateway_mode_v6: routed
    ipam:
      config:
        - subnet: 3d06:bad:b01:0:7ac::/96
          gateway: 3d06:bad:b01:0:7ac::1
